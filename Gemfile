# frozen_string_literal: true

source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

gem "sinatra", "2.0.1"
gem "puma", "4.3.8"
gem 'puma-metrics', '>= 1.1.0'

group :test do
  gem 'rake'
  gem 'rspec'
  gem 'rack-test'
end
